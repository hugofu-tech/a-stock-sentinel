# 已删除 - 此workflow仅用于一次性设置，已执行完毕
# 如需重新设置，请在服务器上执行: bash setup_remote_api.sh
name: Disabled
on: workflow_dispatch
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled'
